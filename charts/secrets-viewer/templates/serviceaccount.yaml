{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "secrets-viewer.serviceAccountName" . }}
  namespace: {{ include "secrets-viewer.namespace" . }}
  labels:
    {{- include "secrets-viewer.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
